package processing;

import queries.Query;
import queries.UnprocessedQuery;

public class QueryProcessor {
	/**
	 * Performs preprocessing on a query in order to retrieve the normalized
	 * term occurences.
	 * 
	 * @param upquery
	 *            The UnprocessedQuery object to process.
	 * @return A processed Query object.
	 */
	public static Query process(UnprocessedQuery upquery) {
		Query result = null;
		/**
		 * TODO Perform processing on the query.
		 */
		return result;
	}
}
